Storage {
    Name = {{ name }}
    WorkingDirectory = "/var/bacula/working"
    Maximum Concurrent Jobs = {{ max_cur_jobs }} 
    SDPort = {{ port }}
    PidDirectory = "/var/run"
    }

Director {
	Name = {{ director_name }}
	Password = "{{director_password}}"
}

Messages {
	Name = Standard
	Director = {{ director_name }} = all
}

@|"sh -c 'for f in /var/nimbus/custom/devices/* ; do echo @${f} ; done'"

